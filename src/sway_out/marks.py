"""Functions for managing marks on containers."""

from i3ipc import Connection

from sway_out.layout_files import WorkspaceLayout

# def apply_marks(connection: Connection, workspace_name: str, workspace_layout: WorkspaceLayout) -> None:
